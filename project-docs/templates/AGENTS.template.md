# AGENTS.md — {{SERVICE_NAME}}

<!-- ИНСТРУКЦИЯ: Компактный ориентир для LLM-агентов. Жёсткий лимит — 150 строк. Только то, что агент не может быстро вывести из кода сам, либо то, где он чаще всего ошибается. Все HTML-комментарии удалить. Детали — не дублировать, а давать ссылки на ARCHITECTURE.md и TESTING.md. -->

> **Версия:** {{VERSION}} | **Обновлено:** {{DATE}}
> Подробно: [ARCHITECTURE.md](ARCHITECTURE.md) · Тестирование и логи: [TESTING.md](TESTING.md)

## Что это за проект

{{TWO_THREE_SENTENCES}}

## Стек

{{ONE_LINE_STACK}}

## Команды

<!-- ИНСТРУКЦИЯ: только реально существующие команды из Makefile/pyproject.toml/package.json/scripts. Проверить каждую перед записью. -->

```bash
{{INSTALL_COMMAND}}     # установка зависимостей
{{RUN_COMMAND}}         # запуск
{{TEST_COMMAND}}        # все тесты
{{LINT_COMMAND}}        # линт/формат
```

## Карта кодовой базы

<!-- ИНСТРУКЦИЯ: 5-12 строк: директория/файл → что там. Только неочевидная навигация; стандартную раскладку фреймворка не описывать. -->

- `{{PATH}}` — {{WHAT_IS_THERE}}

## Границы

<!-- ИНСТРУКЦИЯ: правила вида «всегда/никогда», выведенные из кода и конвенций проекта (миграции, стиль ошибок, запрет прямых обращений к БД вне репозиториев и т.п.). Только подтверждённое. -->

- **Всегда:** {{ALWAYS_RULE}}
- **Никогда:** {{NEVER_RULE}}

## Неочевидные решения (gotchas)

<!-- ИНСТРУКЦИЯ: 3-7 пунктов — архитектурные решения, которые выглядят странно, но намеренны: «финализация пакета делает каждый документ через SELECT FOR UPDATE — координатора нет специально». Это самый ценный раздел для агента. -->

- {{GOTCHA}}

## Где что искать

| Вопрос | Файл |
|---|---|
| Архитектура, пайплайны, интеграции | [ARCHITECTURE.md](ARCHITECTURE.md) |
| Логи, сценарии проверки, статусы | [TESTING.md](TESTING.md) |
| {{KEY_QUESTION}} | `{{KEY_FILE}}` |
