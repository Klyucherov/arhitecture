# Тестирование {{SERVICE_NAME}}

> **Версия:** {{VERSION}}
> **Дата обновления:** {{DATE}}
> **Для кого:** инженеры по тестированию, впервые работающие с сервисом
> **Связанные документы:** [ARCHITECTURE.md](ARCHITECTURE.md) — полное описание архитектуры

<!-- ИНСТРУКЦИЯ: Документ пишется языком, понятным без чтения кода. Каждый grep-паттерн обязан находить реальную строку в коде (проверяется в self-review). Все HTML-комментарии в финальном документе удалить. Неприменимые разделы удалять. -->

---

## Содержание

1. [Что делает сервис](#1-что-делает-сервис)
2. [Окружение и доступы](#2-окружение-и-доступы)
3. [Примеры запросов и ответов](#3-примеры-запросов-и-ответов)
4. [Карта успешных логов](#4-карта-успешных-логов)
5. [Логи ошибок и их трактовка](#5-логи-ошибок-и-их-трактовка)
6. [Как понять состояние обработки](#6-как-понять-состояние-обработки)
7. [Типовые сценарии тестирования](#7-типовые-сценарии-тестирования)
8. [Чеклист успешного прогона](#8-чеклист-успешного-прогона)
9. [На что обращать внимание](#9-на-что-обращать-внимание)

---

## 1. Что делает сервис

<!-- ИНСТРУКЦИЯ: 1 абзац простым языком без жаргона: что приходит на вход, что сервис делает, что получается на выходе. Затем блок «Основной сценарий за N шагов» — нумерованный список из 5-10 шагов на уровне «скачал документ → распознал текст → классифицировал → отправил результат». -->

{{PLAIN_LANGUAGE_DESCRIPTION}}

**Основной сценарий:**

1. {{STEP}}

---

## 2. Окружение и доступы

<!-- ИНСТРУКЦИЯ: откуда запускать (endpoint/топик/команда), какие внешние системы должны быть доступны, какие права нужны. Без реальных хостов и креденшелов — только имена env-переменных и описания. -->

| Что | Где / как |
|---|---|
| Точка входа для запуска обработки | {{ENTRY_POINT}} |
| Внешние зависимости | {{EXTERNAL_DEPENDENCIES}} |

---

## 3. Примеры запросов и ответов

<!-- ИНСТРУКЦИЯ: по подразделу на каждую точку входа (главный HTTP endpoint, сообщение в очереди и т.п.). Примеры брать ТОЛЬКО из кода: модели запросов/ответов, тесты, фикстуры, openapi-спеки (см. discovery.md, раздел 11). Хост заменять на <host>. Если готовых примеров в коде нет — собрать минимальный валидный пример строго по модели/схеме и добавить пометку «пример составлен по модели, в тестах не встречается». Выдуманные поля и значения запрещены. -->

### {{ENTRY_POINT_NAME}}

**Запрос:**

```bash
curl -X {{METHOD}} "<host>{{PATH}}" \
  -H "Content-Type: application/json" \
  -d '{{REQUEST_BODY_EXAMPLE}}'
```

**Успешный ответ (`{{SUCCESS_CODE}}`):**

```json
{{SUCCESS_RESPONSE_EXAMPLE}}
```

**Ошибочные ответы:**

| Код | Когда возникает | Пример тела |
|---|---|---|
| `{{ERROR_CODE}}` | {{WHEN}} | `{{ERROR_BODY}}` |

<!-- ИНСТРУКЦИЯ: для точек входа через очередь вместо curl — пример сообщения: топик и JSON из модели/маппера/теста. -->

**Сообщение в топик `{{TOPIC}}`:**

```json
{{MESSAGE_EXAMPLE}}
```

---

## 4. Карта успешных логов

<!-- ИНСТРУКЦИЯ: ядро документа. Одна строка = одно observable-событие успешного пути, в хронологическом порядке пайплайна. «Паттерн для поиска» — стабильная часть сообщения (без переменных), готовая для grep / запроса в систему логов. Каждый паттерн обязан находиться в коде. Если в логах есть сквозной идентификатор (rq_uid, trace_id, request_id) — указать его первой строкой карты и пояснить, как фильтровать по нему весь прогон. -->

**Как читать таблицу:** находите строку из колонки «Паттерн для поиска» в логах сервиса — значит, соответствующий шаг выполнен успешно.

| # | Шаг пайплайна | Паттерн для поиска | Уровень | Где в коде | Что означает |
|---|---|---|---|---|---|
| 1 | {{PIPELINE_STEP}} | `{{GREP_PATTERN}}` | {{LEVEL}} | `{{FILE_LINE}}` | {{MEANING}} |

---

## 5. Логи ошибок и их трактовка

<!-- ИНСТРУКЦИЯ: таблица ошибочных log-событий: паттерн, когда возникает, критичность (останавливает ли обработку всего пакета/запроса или только одну единицу данных), что делать тестировщику (баг / ожидаемое поведение / проблема окружения). -->

| Паттерн для поиска | Когда возникает | Критичность | Что делать |
|---|---|---|---|
| `{{GREP_PATTERN}}` | {{WHEN}} | {{SEVERITY}} | {{ACTION}} |

---

## 6. Как понять состояние обработки

<!-- ИНСТРУКЦИЯ: если есть БД со статусной моделью — таблица статусов: статус, что значит, терминальный ли. Если состояние видно только в логах — описать, по каким логам отслеживать прогресс. Указать сквозные идентификаторы (пакет/документ/запрос) и как по ним искать. Если неприменимо — удалить раздел. -->

| Статус | Что значит | Терминальный |
|---|---|---|
| `{{STATUS}}` | {{MEANING}} | {{YES_NO}} |

---

## 7. Типовые сценарии тестирования

<!-- ИНСТРУКЦИЯ: 3-7 сценариев. Обязательны: happy path; недоступность каждой критичной внешней системы; таймаут/зависание асинхронного шага (если есть); повторная отправка тех же данных (идемпотентность, если заявлена в коде). Для каждого сценария: шаги, ожидаемые логи (ссылка на раздел 3/4), ожидаемый конечный статус/результат. -->

### Сценарий 1. {{SCENARIO_NAME}} (happy path)

1. {{ACTION}}

**Ожидаемый результат:** {{EXPECTED}}

**Контроль по логам:** {{LOG_ANCHORS}}

---

## 8. Чеклист успешного прогона

<!-- ИНСТРУКЦИЯ: короткий итоговый чеклист «прогон считается успешным, если»: 4-8 пунктов, каждый проверяем (лог/статус в БД/сообщение в очереди/файл в хранилище). -->

Прогон считается успешным, если:

- [ ] {{CHECK}}

---

## 9. На что обращать внимание

<!-- ИНСТРУКЦИЯ: 3-8 маркированных пунктов о неочевидном поведении, которое легко принять за баг: осознанные ретраи, задержки watchdog, «мягкие» ошибки, при которых сервис отвечает успехом, особенности параллелизма (порядок логов не гарантирован) и т.п. Только то, что подтверждено кодом. -->

- {{GOTCHA}}
